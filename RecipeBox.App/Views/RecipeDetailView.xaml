<UserControl x:Class="RecipeBox.App.Views.RecipeDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RecipeBox.App.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">
            <TextBlock Text="Create a New Recipe" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

            <TextBlock Text="Title"/>
            <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,15"/>

            <TextBlock Text="Instructions"/>
            <TextBox Text="{Binding Instructions, UpdateSourceTrigger=PropertyChanged}" 
                 Height="200" TextWrapping="Wrap" AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto" Margin="0,5,0,15"/>

            <TextBlock Text="Ingredients" Margin="0,15,0,0"/>
            <ListView ItemsSource="{Binding Ingredients}" 
                        SelectedItem="{Binding SelectedIngredient}"
                        Height="150" Margin="0,5,0,0">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Ingredient" Width="200" 
                            DisplayMemberBinding="{Binding Ingredient.Name}"/>

                        <GridViewColumn Header="Quantity" Width="150" 
                            DisplayMemberBinding="{Binding Quantity}"/>
                    </GridView>
                </ListView.View>
            </ListView>
            <StackPanel Orientation="Horizontal" Margin="0,5,0,15">
                <Button Content="Add Ingredient" Command="{Binding AddIngredientCommand}" Width="120"/>
                <Button Content="Remove Selected" Command="{Binding RemoveIngredientCommand}" Width="120" Margin="5,0,0,0"/>
            </StackPanel>
            <!-- We'll enable this later based on user role -->
            <CheckBox Content="Make this recipe public" IsChecked="{Binding IsPublic}"
                       IsEnabled="{Binding CanMakePublic}"/>

            <StackPanel Orientation="Horizontal" Margin="0,20,0,0">
                <Button Content="Save" Command="{Binding SaveCommand}" Width="100"/>
                <Button Content="Cancel" Command="{Binding CancelCommand}" Width="100" Margin="10,0,0,0"/>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</UserControl>
