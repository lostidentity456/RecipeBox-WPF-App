<Window x:Class="RecipeBox.App.Views.AddIngredientDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RecipeBox.App.Views"
        mc:Ignorable="d"
        Title="Add Ingredient" SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        WindowStyle="ToolWindow" ResizeMode="NoResize">
    <StackPanel Margin="15">
        <TextBlock Text="Ingredient Name:"/>
        <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Width="250" Margin="0,5,0,0"/>

        <TextBlock Text="Quantity:" Margin="0,15,0,0"/>
        <TextBox Text="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}" Width="250" Margin="0,5,0,0"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="OK" Width="80" IsDefault="True" Click="OkButton_Click"/>
            <Button Content="Cancel" Width="80" IsCancel="True" Margin="5,0,0,0"/>
        </StackPanel>
    </StackPanel>
</Window>
